# .zprofile is loaded for all login shells, regardless of interactivity
# Sourced for all new terminal windows and ssh connections
# NOT sourced for sub-shells (running zsh again within an already executing shell)
#   (That said, many settings would still be inherited but the file is not re-sourced)
# https://mac.install.guide/terminal/zshrc-zprofile

# Most commonly used for PATH, EDITOR, and some other environment variables

#########################
# Homebrew config
#########################
# Environment, path, etc
eval "$(/opt/homebrew/bin/brew shellenv)"

export PATH=~/bin:$PATH

export EDITOR=nano
